# 更新日志 / Update Log

本项目从 `MaterialTodo` 更名为 **Easy-TodoList**。以下版本记录从更名后的首个正式版开始。

---

## v1.4.2 (2026-08-17)

### 优化 Improvements

- 更新版本号


## v1.4.1 (2026-08-17)

### 优化 Improvements

- 修复了一些bug

## v1.4.0 (2026-08-17)

### 优化 Improvements

- 优化发布release notes 读取识别逻辑


## v1.3.0 (2026-08-17)

### 优化 Improvements

- 优化版本识别逻辑


## v1.2.0 (2026-08-17)

### 新增 Features

- 新增版本功能，增加显示当前软件版本号于设置界面最底部

### 优化 Improvements

- 优化版本打包逻辑


## v1.1.0 (2026-08-17)

### 新增 Features

- 重复运行检测（单实例守护）：再次启动程序时不会新开第二个实例，而是把已有实例的主窗口唤起（普通启动 / `--show`），或保持隐藏（`--hidden` / `--tray`），随后自动退出本次启动。

### 优化 Improvements

- 静默后台运行：窗口改为工具窗口（Tool），底部任务栏完全不显示图标，仅保留右下角系统托盘图标，彻底避免任务栏残留。

### 修复 Fixes

- 修复开机自启动时窗口被直接最小化到托盘、未默认展示界面的问题：开机自启不再强制 `--hidden` 启动参数，登录后默认弹出待办界面（是否启动即隐藏改由设置里的「启动时隐藏到托盘」统一控制）。

---

## v1.0.0 (2026-08-17)

首个正式版：按产品设计说明整体重构为「单窗口、上下两个圆角悬浮面板」的桌面待办小组件，并统一命名为 Easy-TodoList。

### 新增 Features

- 界面重构：顶部信息栏 + 下方主内容面板，上下两个圆角悬浮面板。
- 顶部信息栏：应用 Logo + 「N 个待办事项」动态计数 + 彩色加号添加按钮 + 彩色齿轮设置按钮。
- 主内容面板：「全部待办」标题 + 内嵌输入框（回车即添加）+ 单行待办列表。
- 待办行：点击整行标记完成（划横线置灰），悬停显示 ✏️ 编辑 / ❌ 删除按钮。
- 右下角圆形悬浮按钮：打勾（全部标记完成）、打叉（清除已完成，点击即执行、无二次确认）。
- 设置视图（整体替换主内容区，可滚动）：磨砂透明度、窗体圆角大小、桌面置顶、显示/收起顶部栏、锁定位置（禁止拖动）、锁定大小（禁止缩放）、开机自启、启动/关闭托盘行为、全局快捷键。
- 全局快捷键：唤起组件 / 新建待办（Windows `RegisterHotKey`，可点击录制）。
- 右下角拖动缩放窗口。
- 顶部栏收起时，「全部待办」旁自动显示齿轮入口，保证仍能进入设置恢复顶部栏。
- 首次启动自动迁移旧版 `MaterialTodo` 数据目录到 `Easy-TodoList`。

### 优化 Improvements

- 磨砂改为 DWM 区域模糊（`DwmEnableBlurBehindWindow` + `DWM_BB_BLURREGION`），严格裁剪在圆角面板内部，圆角外与中间空隙完全透明，消除方形磨砂泄露与残留阴影。
- 全部图标改为 QPainter 矢量绘制（设置/加号为彩色实心，编辑为铅笔、删除为叉号），消除 emoji 字形发虚。
- 待办文字适度加粗，低透明度下更清晰。
- 移除主图标上的红色数量角标，仅保留「N 个待办事项」文字。
- 移除待办添加的二次确认与挤压布局问题，添加路径一步完成。

### 修复 Fixes

- 修复磨砂背景溢出圆角裁剪范围、四角方形模糊底色。
- 修复中间透明空隙未生效。
- 修复顶栏收起后无法重新打开设置的问题。
- 修复点击添加按钮导致界面被透明空隙拆分的问题。
